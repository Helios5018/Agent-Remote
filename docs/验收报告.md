# CMUX Agent Remote 第一版验收报告

验收时间：2026-08-17
验收环境：macOS (Darwin 25.6.0) / Bun 1.3.11 / cmux（真实运行中，9 个真实 Agent）

---

## 1. MVP 验收标准（需求文档 §33）

| # | 验收项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | 能自动获取所有 cmux Workspace | ✅ | `GET /api/tree` 返回真实 7 个 workspace；`discovery.test.ts` |
| 2 | 能获取 Workspace 下所有 Surface | ✅ | 同上，含非 Agent 的 shell surface |
| 3 | 能识别 Claude / Codex / Grok | ✅ | 真机识别出 claude×3 / codex×3 / grok×3；进程名被改成版本号时靠 cmux `coding_agents` pid 列表兜底 |
| 4 | 能准确把 Agent 与 Surface 绑定 | ✅ | surface UUID 为主键，pid → surface 反查表覆盖整棵进程树 |
| 5 | 能读取指定 Surface 输出 | ✅ | `GET /api/surfaces/:id/output`，base64 全屏 + ANSI 清洗 |
| 6 | 能向指定 Surface 输入 Prompt | ✅ | 浏览器输入中文 prompt → cmux surface 收到（见 §2 闭环） |
| 7 | 能发送 Enter / Esc / Ctrl+C | ✅ | 六个键白名单；非法键 400 |
| 8 | 三个 Agent 均可通过 Hook 上报状态 | ✅ | 真实 shell 脚本 → HTTP → adapter → 状态置顶（见 §3） |
| 9 | 能显示 Working / Needs You / Responded / Idle | ✅ | 首页三组 + 汇总「6 Need You · 1 Working · 3 Idle」 |
| 10 | Responded 支持未读状态 | ✅ | 打开会话 RESPONDED_UNREAD → IDLE；正在查看时回合结束直接算已读 |
| 11 | 首页按 Attention Priority 排序 | ✅ | `protocol.test.ts` 锁定顺序；真机首页置顶为 NEEDS_YOU |
| 12 | 手机浏览器可以正常使用 | ✅ | 390×844 视口实测，见截图 01/02/03 |
| 13 | WebSocket 断线后可以恢复 | ✅ | 停服 → 横幅 + 退化轮询；重启 → 自动重连且无需重新登录（见 §4） |
| 14 | 所有写操作必须明确指定 Surface | ✅ | 路径强制带 surfaceId；`assertSurfaceTarget` 双重校验；缺失直接 404 |
| 15 | 系统具有基础访问 Token | ✅ | Token → HttpOnly Cookie；未登录全部 401 |
| 16 | Hook 服务失效不会影响 Agent 本身运行 | ✅ | Hook 接口一律 200；hook 脚本在服务不可用/无配置/非法输入时 exit 0 且 stdout 为空 |

---

## 2. V0 完成标准闭环（需求文档 §24 / §32）

真机实测（cmux workspace「CAR E2E Test」中运行一个进程名为 `codex` 的测试 Agent）：

```text
浏览器输入：很好，继续检查一下边界 case，然后提交 commit。
   ↓  POST /api/surfaces/DC4822B4-…/input  (control mode)
cmux surface:62 收到并执行
   ↓
终端输出变化
   ↓  WebSocket surface.snapshot rev=3
浏览器立即看到 [agent] received: 很好，继续检查一下边界 case，然后提交 commit。
```

状态流转（真机）：

```text
IDLE → （Web 发送 prompt）→ WORKING · Thinking
     → （输出静止 15s）    → RESPONDED_UNREAD（活动文案已清空）
     → （打开会话）        → IDLE
```

首页在同一时刻出现：`CAR E2E Test / Codex / ◇ Responded`（截图 01）。

---

## 3. Hook 链路真机验证

用真实的 `scripts/cmux-agent-web-hook` 脚本，对一个真实 Grok surface 上报：

```bash
echo '{"session_id":"live-test","tool_name":"bash",
       "message":"Grok needs your permission to use bash"}' |
  CMUX_SURFACE_ID=5877E341-… ./scripts/cmux-agent-web-hook grok Notification
# exit 0
```

结果：该 Agent 立刻变成 `NEEDS_APPROVAL`，活动文案为原始通知内容，并排到首页 NEEDS YOU 首位；
`hookConnected` 置为 true（之后不再依赖输出推断）。随后 `Stop` → `RESPONDED_UNREAD`，
`POST /api/agents/:id/read` → `IDLE`。

失效场景（全部返回 200 / exit 0，不影响 Agent）：
token 错误、未知 agent、非法 JSON、未知事件名、关联不到 surface、服务完全没启动、没有配置文件。

---

## 4. WebSocket 断线恢复真机验证

| 时刻 | 动作 | 页面状态 |
|------|------|----------|
| T0 | 正常连接 | `conn-open`，无横幅 |
| T1 | `tmux kill-session` 停掉服务 6s | `conn-closed`，横幅「连接中断，正在自动重连…（已退化为轮询）」 |
| T2 | 重启服务 | 约 5s 内自动 `conn-open`，横幅消失，10 个 Agent 正常显示，**无需重新输入 Token** |

（Session 持久化在 SQLite 中，但控制模式不持久化 —— 重启后一律回到只读。）

---

## 5. 安全项验证

| 项 | 验证 |
|----|------|
| 未登录 | `/api/agents`、`/api/tree`、`/api/surfaces/:id/output`、`/api/audit` 全部 401 |
| 只读模式写入 | 403 `READ_ONLY`，且 cmux 端确认没有收到任何输入 |
| 控制模式超时 | TTL 到期后写操作 403；期间持续操作会续期 |
| Ctrl+C | 未确认 428 `CONFIRM_REQUIRED`；带 `confirm:true` 才真正发送 |
| 非法按键 | `ctrl+d` / `f5` / 空串 → 400 |
| 命令注入 | prompt 含 `$(rm -rf /)` 与 `--help` 时按字面文本传递（参数数组 + `--`） |
| 审计 | 记录 `surface.input len=19 submit=true`，**不含 prompt 原文** |
| 目录穿越 | 静态文件服务对 `..` 路径返回 403 |

---

## 6. 自动化测试

```text
 ✓ apps/server/test/protocol.test.ts       (14)  统一状态 / 事件 / 按键白名单 / 文本清洗 / 时间
 ✓ apps/server/test/discovery.test.ts       (7)  cmux 拓扑解析 + Agent 进程发现 + pid 反查
 ✓ apps/server/test/cli-client.test.ts     (12)  CLI 封装、缓存、single-flight、ANSI、防注入
 ✓ apps/server/test/hooks.test.ts          (25)  claude / codex / grok 三家 adapter 归一
 ✓ apps/server/test/engine.test.ts         (28)  状态机 / 关联 / 未读 / stale / 排序 / 持久化
 ✓ apps/server/test/store.test.ts           (6)  SQLite 落盘、审计、不存终端内容
 ✓ apps/server/test/realtime.test.ts       (10)  Hub 订阅广播 + Poller 分级刷新
 ✓ apps/server/test/api.test.ts            (25)  鉴权 / 只读 / 危险键 / Hook / 审计 / 实时联动
 ✓ apps/server/test/security.test.ts       (17)  Token / Session / 控制模式 / 命令行参数
 ✓ apps/server/test/install-hooks.test.ts  (12)  三家 hook 安装、合并、备份、幂等卸载
 ✓ apps/server/test/hook-script.test.ts     (7)  真实执行 shell hook 脚本（含服务不可用场景）

 Test Files  11 passed (11)
      Tests  163 passed (163)
```

`tsc --noEmit` 全量通过；前端 `vite build` 通过（gzip 后 JS 65.9 kB / CSS 2.1 kB）。

---

## 7. 已知限制（第一版不做，见需求文档 §3.2）

- 不是完整 Terminal Emulator：输出以纯文本 `pre` 渲染（手机可读性优先）。
- 没有安装 Hook 时，状态靠输出变化推断：TUI 动画多的 Agent 可能出现短暂 `WORKING` 误判；
  装上 Hook 后完全以 Hook 为准。
- 单用户单 Token，无多用户协作、无云端 Relay（V2 范围）。
- tmux 长任务、Project 聚合属于 V1 范围，本版未实现。
- Session 持久化在本机 SQLite；`--rotate-token` 可一键作废所有设备。

---

## 8. 截图

| 文件 | 说明 |
|------|------|
| `docs/screenshots/01-inbox-mobile.png` | 手机端 Attention Inbox（真实 10 个 Agent） |
| `docs/screenshots/02-session-mobile.png` | 手机端 Agent 会话页（真实 Claude Code 输出） |
| `docs/screenshots/03-session-control-mode.png` | 控制模式 + 中文输入 |
| `docs/screenshots/04-workspace-tree.png` | Workspace → Pane → Surface 结构 |
| `docs/screenshots/05-session-desktop.png` | 桌面宽度布局 |
